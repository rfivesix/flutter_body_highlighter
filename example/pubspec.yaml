name: flutter_body_highlighter_example
description: Example app for flutter_body_highlighter.
publish_to: none
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_body_highlighter:
    path: ../

dev_dependencies:
  flutter_lints: ^3.0.0
